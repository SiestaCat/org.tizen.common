package org.tizen.common.util.url.cp;

import java.net.URL;
import org.tizen.common.util.url.classpath.Connection;

public class Connection extends Connection {
  protected Connection(URL url, String fqcn) {
    super(url, fqcn);
  }
}
